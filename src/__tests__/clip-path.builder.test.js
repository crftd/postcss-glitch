/**
 * Crafted by Crash on 14.12.17.
 */

import * as builder from '../clip-path.builder';

describe('clip-path builder', () => {
  it('throws unimplemented error', () => {
    // Arrange
    // Act
    // Assert
    expect(builder.default).toThrowError('Not implemented');
  });
});
